version: '3.9'

# Stream Processing Engines
# Flink: Real-time stream processing
# Spark: Unified batch and stream processing

services:
  # === Flink Cluster ===
  flink-jobmanager:
    image: flink:1.18.0-scala_2.12-java11
    container_name: meeting-flink-jobmanager
    ports:
      - "8082:8081"
    command: jobmanager
    environment:
      - |
        FLINK_PROPERTIES=
        jobmanager.rpc.address: flink-jobmanager
        taskmanager.numberOfTaskSlots: 2
        parallelism.default: 2
        state.backend: filesystem
        state.checkpoints.dir: file:///tmp/flink-checkpoints
        state.savepoints.dir: file:///tmp/flink-savepoints
        execution.checkpointing.interval: 60000
        execution.checkpointing.min-pause: 10000
    volumes:
      - flink_checkpoints:/tmp/flink-checkpoints
      - flink_savepoints:/tmp/flink-savepoints
      - ./flink_jobs:/opt/flink/jobs
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/overview"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - rag-network

  flink-taskmanager:
    image: flink:1.18.0-scala_2.12-java11
    container_name: meeting-flink-taskmanager
    depends_on:
      flink-jobmanager:
        condition: service_healthy
    command: taskmanager
    environment:
      - |
        FLINK_PROPERTIES=
        jobmanager.rpc.address: flink-jobmanager
        taskmanager.numberOfTaskSlots: 2
        taskmanager.memory.process.size: 1728m
    volumes:
      - flink_checkpoints:/tmp/flink-checkpoints
      - flink_savepoints:/tmp/flink-savepoints
      - ./flink_jobs:/opt/flink/jobs
    networks:
      - rag-network

  # === Spark Cluster ===
  spark-master:
    image: apache/spark:3.5.0
    container_name: meeting-spark-master
    ports:
      - "8084:8080"
      - "7077:7077"
    command: /opt/spark/bin/spark-class org.apache.spark.deploy.master.Master
    environment:
      - SPARK_MASTER_HOST=spark-master
      - SPARK_MASTER_PORT=7077
      - SPARK_MASTER_WEBUI_PORT=8080
      - SPARK_LOCAL_IP=spark-master
    volumes:
      - spark_master_data:/opt/spark/work
      - ./spark_jobs:/opt/spark/jobs
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8080"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - rag-network

  spark-worker-1:
    image: apache/spark:3.5.0
    container_name: meeting-spark-worker-1
    depends_on:
      spark-master:
        condition: service_healthy
    command: /opt/spark/bin/spark-class org.apache.spark.deploy.worker.Worker spark://spark-master:7077
    environment:
      - SPARK_WORKER_CORES=2
      - SPARK_WORKER_MEMORY=2g
      - SPARK_LOCAL_IP=spark-worker-1
    volumes:
      - spark_worker_1_data:/opt/spark/work
      - ./spark_jobs:/opt/spark/jobs
    networks:
      - rag-network

  spark-worker-2:
    image: apache/spark:3.5.0
    container_name: meeting-spark-worker-2
    depends_on:
      spark-master:
        condition: service_healthy
    command: /opt/spark/bin/spark-class org.apache.spark.deploy.worker.Worker spark://spark-master:7077
    environment:
      - SPARK_WORKER_CORES=2
      - SPARK_WORKER_MEMORY=2g
      - SPARK_LOCAL_IP=spark-worker-2
    volumes:
      - spark_worker_2_data:/opt/spark/work
      - ./spark_jobs:/opt/spark/jobs
    networks:
      - rag-network

volumes:
  flink_checkpoints:
  flink_savepoints:
  spark_master_data:
  spark_worker_1_data:
  spark_worker_2_data:

networks:
  rag-network:
    external: true

