version: '3.9'

services:
  mongodb:
    image: mongo:6.0
    container_name: mongodb
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: quanmd
      MONGO_INITDB_ROOT_PASSWORD: quanmd
    volumes:
      - ./mongo_data:/data/db